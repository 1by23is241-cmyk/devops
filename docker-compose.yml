version: '3.8'

services:
  web:
    build: .
    container_name: fittrack-app
    ports:
      - "5000:5000"
    volumes:
      - gym_data:/app
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

volumes:
  gym_data:
